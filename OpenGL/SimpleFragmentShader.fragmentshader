#version 330 // OpenGL Version 3.3 min req

in vec3 retColor;   // MUST use same name as out variable used by vertex shader
in vec2 retTexCoord;

out vec4 FragColor;

uniform sampler2D sampler1;

void main()
{
    FragColor = texture(sampler1, retTexCoord); // example 1: texture only
}